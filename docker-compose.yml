services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "8106:80"
    depends_on:
      - api

  api:
    build:
      context: api
    restart: unless-stopped
    env_file: .env
